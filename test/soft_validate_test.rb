require 'test_helper'

class SoftValidateTest < ActiveSupport::TestCase
  test "truth" do
    assert_kind_of Module, SoftValidate
  end

  test "a soft validated record will have a #soft_valid?" do
    
  end
end
